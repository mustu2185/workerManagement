<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anahad.projectmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MobileNumberAuth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProjectListView" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".SignIn" />
        <activity android:name=".HouseHoldList" />
        <activity android:name=".HouseHoldView" />
        <activity android:name=".ProjectNewView" />
        <activity android:name=".ProjectView" />
        <activity android:name=".ProjectUserListView"></activity>
    </application>

</manifest>